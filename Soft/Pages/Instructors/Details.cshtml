@page
@model ContosoUniversityWithRazor.Pages.Instructors.DetailsModel
@using ContosoRazorKoopia.Pages.Extensions;

@{
    Layout = "_Details";
    ViewData["Title"] = "Details";
    ViewData["Page"] = "Instructors";
    ViewData["ItemId"] = Model.Instructor.ID;
}

@Html.Show(model => model.Instructor.HireDate)
        
@Html.Show(model => model.Instructor.LastName)

@Html.Show(model => model.Instructor.FirstMidName)